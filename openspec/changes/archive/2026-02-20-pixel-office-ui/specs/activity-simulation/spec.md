## ADDED Requirements

### Requirement: Activity State Machine

Каждый персонаж SHALL иметь state machine с состояниями: idle, moving, working, break.

#### Scenario: Переходы состояний

- **WHEN** персонаж завершает текущую активность (таймер истёк)
- **THEN** выбирается случайная следующая активность из списка доступных для данной роли
- **AND** персонаж переходит в состояние `moving` и движется к целевой зоне
- **AND** по прибытии переходит в `working` или `break`

#### Scenario: Длительность активностей

- **WHEN** персонаж начинает активность
- **THEN** длительность выбирается случайно в диапазоне 5–20 секунд для работы и 3–8 секунд для перерывов

### Requirement: Role-Based Activities

Каждая роль SHALL иметь специфические активности.

#### Scenario: CEO активности

- CEO Алекс выполняет: "📊 Анализирует метрики", "📞 Звонок с инвестором", "💬 1:1 встреча", "✉️ Пишет email", "☕ Кофе-брейк"

#### Scenario: Dev активности

- Dev Макс выполняет: "💻 Кодит", "🐛 Дебажит", "📖 Читает доки", "🖨️ Принтер", "💬 Code review", "☕ Кофе"

#### Scenario: Accountant активности

- Accountant Лена выполняет: "📊 Считает", "📋 Отчёт", "💰 Бюджет", "🖨️ Печатает", "☕ Чай", "📞 Звонок"

#### Scenario: Manager активности

- Manager Катя выполняет: "📋 Планирует", "💬 Совещание", "📧 Email", "🗓️ Календарь", "☕ Кофе", "🖨️ Принтер"

### Requirement: WebSocket Event Integration Point

Архитектура симуляции SHALL предусматривать возможность подключения внешних событий.

#### Scenario: Внешнее событие переопределяет симуляцию

- **WHEN** приходит внешнее событие `{agentId, activity, status}`
- **THEN** соответствующий персонаж немедленно переходит к указанной активности
- **AND** симуляция для этого персонажа приостанавливается до завершения внешнего события
